<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{title}}</h1>
    <div id="header">
      <div id="status-msg-header">
        {{#if user}}
          Welcome, {{user.username}}!
        {{/if}}
      </div>
      <div id="nav-links">
        {{#if user}}
          {{#if user.isTeacher}}
            <a href="/results/">Student Progress</a>
            <a href="/results/json">Student Progress JSON</a>
            <a href="/accounts/add-student">Add Student</a>
            <!-- <a href="/account/updatey">Account</a> -->
            <a href="/accounts/logout">Logout</a>
          {{/if}}
          {{#unless user.isTeacher}}
            <a href="/">Play</a>
            <a href="/account/modify">Account</a>
            <a href="/accounts/logout">Logout</a>
          {{/unless}}
        {{/if}}
        {{#unless user}}
          <a href="/accounts/register">Register New Teacher</a>
        {{/unless}}
      </div>
      <div>
        {{#unless user }}
        <form id="login" action="/accounts/login" method="post">
          <input type="text" name="username" value=""  placeholder="Username">
          <input type="password" name="password" value="" placeholder="Password">
          <input type="submit" value="Login">
        </form>
        {{/unless}}
      </div>

    </div>
    <div>
      {{{body}}}
    </div>
  </body>
</html>
